{# Templates extend from base template  #}
{% extends 'base.html.twig' %}

{% block title %}Bestellung Edit{% endblock %}

{% block body %}
    {{ include('Navigation/_horizoltal_nav_bar.html.twig') }}
    <div class="main">
        {{ include('Navigation/_vertical_nav_bar.html.twig') }}
        <div class="projekt">

            <div id="editBestellung" data-csrf-token="{{ csrf_token('edit_bestellung') }}">
                <bestellung-edit></bestellung-edit>
            </div>

        </div>
    </div>

{% endblock %}
