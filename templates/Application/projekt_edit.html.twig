{# Templates extend from base template  #}
{% extends 'base.html.twig' %}

{% block title %}Projekt{% endblock %}

{% block body %}
    {{ include('Navigation/_horizoltal_nav_bar.html.twig') }}
    <div class="main">
        {{ include('Navigation/_vertical_nav_bar.html.twig') }}
        <div class="projekt">
{#            // id="editProject" --> will be use in Vue (UI) / token --> server (Symfony)#}
            <div id="editProject" data-csrf-token="{{ csrf_token('edit_projekte') }}">
                <project-edit></project-edit>
            </div>

        </div>
    </div>

{% endblock %}
